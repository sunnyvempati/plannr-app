<% content_for(:title) { @event.name } %>
<% content_for(:header) { @event.name } %>

<%= react_component("EventDashboardRouter",
                    event: @event,
                    client: @event.client,
                    authToken: form_authenticity_token,
                    currentUser: current_user )%>
