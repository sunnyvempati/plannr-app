<% session[:return_to] ||= request.referer %>

<%= react_component("ContactFormNew",
                    model: @contact,
                    authToken: form_authenticity_token,
                    secondaryButtonHref: session[:return_to]

    ) %>


