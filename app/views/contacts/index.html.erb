<% content_for(:title) { "#{current_user.company.name}'s Contacts" } %>
<% content_for(:header) { "Contacts" } %>

<%= react_component("ContactsRouter", currentUser: current_user, prerender: true) %>
