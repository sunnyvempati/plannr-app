<% content_for(:header) { "Edit Contact" } %>

<%= react_component("ContactForm",
                    model: @contact,
                    actionVerb: "PUT",
                    authToken: form_authenticity_token
) %>
