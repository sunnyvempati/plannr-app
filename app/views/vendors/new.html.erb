<% content_for(:header) { "New Vendor" } %>

<%= react_component("VendorFormNew",
                    model: @vendor,
                    notice: notice,
                    authToken: form_authenticity_token
    ) %>
