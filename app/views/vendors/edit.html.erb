<% content_for(:header) { "Edit Vendor" } %>

<%= react_component("VendorFormEdit",
                    model: @vendor,
                    routeVerb: 'PUT',
                    authToken: form_authenticity_token
) %>
