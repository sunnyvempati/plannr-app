<%= react_component("SignUpForm", authToken: form_authenticity_token,
                                  email: @user.email,
                                  company: @user.company,
                                  invite_token: @invitation.token) %>
