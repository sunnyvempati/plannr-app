<% content_for(:title) { current_user.company.name } %>
<% content_for(:header) { current_user.company.name } %>

<%= react_component("InviteUsers",
            authToken: form_authenticity_token) %>

<%= react_component("CompanyUserTable") %>
