<%= react_component("CompanyUserTable") %>

<%= react_component("InviteUsers", admin: @current_user.company_admin?,
                                   currentUserId: @current_user.id,
                                   authToken: form_authenticity_token) %>
